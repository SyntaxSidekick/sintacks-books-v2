# Worker

The worker owns background job execution. Queue names use the
`sintacks.<domain>.<purpose>` convention. Stage 1 includes only an
infrastructure-verification heartbeat job and no product automation.
